models:
  - name: bronze_raw_loans
    columns:
      - name: income_str
        tests:
          - not_null
version: 2
models:
  - name: bronze_raw_loans
    columns:
      - name: credit_score
        tests:
          - not_null
models:
  - name: bronze_raw_loans
    columns:
      - name: application_id
        tests:
          - not_null